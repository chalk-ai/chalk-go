// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: chalk/remotetty/v1/service.proto

package remotettyv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/chalk-ai/chalk-go/gen/chalk/remotetty/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RemoteTTYServiceName is the fully-qualified name of the RemoteTTYService service.
	RemoteTTYServiceName = "chalk.remotetty.v1.RemoteTTYService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RemoteTTYServiceCreateDebugTTYProcedure is the fully-qualified name of the RemoteTTYService's
	// CreateDebugTTY RPC.
	RemoteTTYServiceCreateDebugTTYProcedure = "/chalk.remotetty.v1.RemoteTTYService/CreateDebugTTY"
)

// RemoteTTYServiceClient is a client for the chalk.remotetty.v1.RemoteTTYService service.
type RemoteTTYServiceClient interface {
	// CreateDebugTTY establishes a bidirectional streaming TTY session to a pod
	// The first request must contain init_request with cluster, namespace, and pod information
	// Subsequent requests contain input data to send to the TTY stdin
	// Responses contain output data from the TTY stdout/stderr
	CreateDebugTTY(context.Context) *connect.BidiStreamForClient[v1.CreateDebugTTYRequest, v1.CreateDebugTTYResponse]
}

// NewRemoteTTYServiceClient constructs a client for the chalk.remotetty.v1.RemoteTTYService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRemoteTTYServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RemoteTTYServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	remoteTTYServiceMethods := v1.File_chalk_remotetty_v1_service_proto.Services().ByName("RemoteTTYService").Methods()
	return &remoteTTYServiceClient{
		createDebugTTY: connect.NewClient[v1.CreateDebugTTYRequest, v1.CreateDebugTTYResponse](
			httpClient,
			baseURL+RemoteTTYServiceCreateDebugTTYProcedure,
			connect.WithSchema(remoteTTYServiceMethods.ByName("CreateDebugTTY")),
			connect.WithClientOptions(opts...),
		),
	}
}

// remoteTTYServiceClient implements RemoteTTYServiceClient.
type remoteTTYServiceClient struct {
	createDebugTTY *connect.Client[v1.CreateDebugTTYRequest, v1.CreateDebugTTYResponse]
}

// CreateDebugTTY calls chalk.remotetty.v1.RemoteTTYService.CreateDebugTTY.
func (c *remoteTTYServiceClient) CreateDebugTTY(ctx context.Context) *connect.BidiStreamForClient[v1.CreateDebugTTYRequest, v1.CreateDebugTTYResponse] {
	return c.createDebugTTY.CallBidiStream(ctx)
}

// RemoteTTYServiceHandler is an implementation of the chalk.remotetty.v1.RemoteTTYService service.
type RemoteTTYServiceHandler interface {
	// CreateDebugTTY establishes a bidirectional streaming TTY session to a pod
	// The first request must contain init_request with cluster, namespace, and pod information
	// Subsequent requests contain input data to send to the TTY stdin
	// Responses contain output data from the TTY stdout/stderr
	CreateDebugTTY(context.Context, *connect.BidiStream[v1.CreateDebugTTYRequest, v1.CreateDebugTTYResponse]) error
}

// NewRemoteTTYServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRemoteTTYServiceHandler(svc RemoteTTYServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	remoteTTYServiceMethods := v1.File_chalk_remotetty_v1_service_proto.Services().ByName("RemoteTTYService").Methods()
	remoteTTYServiceCreateDebugTTYHandler := connect.NewBidiStreamHandler(
		RemoteTTYServiceCreateDebugTTYProcedure,
		svc.CreateDebugTTY,
		connect.WithSchema(remoteTTYServiceMethods.ByName("CreateDebugTTY")),
		connect.WithHandlerOptions(opts...),
	)
	return "/chalk.remotetty.v1.RemoteTTYService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RemoteTTYServiceCreateDebugTTYProcedure:
			remoteTTYServiceCreateDebugTTYHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRemoteTTYServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRemoteTTYServiceHandler struct{}

func (UnimplementedRemoteTTYServiceHandler) CreateDebugTTY(context.Context, *connect.BidiStream[v1.CreateDebugTTYRequest, v1.CreateDebugTTYResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("chalk.remotetty.v1.RemoteTTYService.CreateDebugTTY is not implemented"))
}
