comment:
  layout: "condensed_header, condensed_files, condensed_footer"
  hide_project_coverage: true

coverage:
  ignore:
    - "gen/chalk/"
    - "gen/gen_bq_schema/"
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto